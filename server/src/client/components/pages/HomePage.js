import React from 'react';

const Home = () => (
  <div>
    <div> Home componentss </div>
    <button onClick={() => console.log('hit here')}>Press me! </button>
  </div>
);

export default {
  component: Home,
};
